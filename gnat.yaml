server:
  bind_port: 8080
  public_url: "http://localhost:8080"

database:
  driver: sqlite
  path: "./analytics.db"

api_key: "dev-secret-change-me"

retention:
  raw_events_days: 0

updates:
  check_for_updates: true
